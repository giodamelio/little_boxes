language: rust
rust:
  - stable
  - beta
  - nightly
before_script:
  - rustup component add clippy || cargo install --git https://github.com/rust-lang/rust-clippy/ --force clippy
script:
  - cargo clippy
  - cargo test
matrix:
  allow_failures:
    - rust: nightly
